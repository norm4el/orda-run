# В процессе (In Progress)

Текущие задачи, которые частично реализованы на бэкенде, но требуют доработки или интеграции в пользовательский интерфейс мобильного приложения, а также срочные багфиксы.

## 1. Исправление критических UI-багов (Срочно)
- Убрать сырые переменные в UI Flutter. На нескольких экранах видны баги парсинга строк: `${index + 1}`, `${q['reward']}`, `${currentUser.influencePoints}`. Для пользователя это выглядит как сломанное приложение. Это нужно исправить до любого масштабирования.

## 2. Полировка дизайна (Повышение премиальности) [ВЫПОЛНЕНО]
- **Цветовая палитра**: Текущий золотой акцент (`#d8a760`) заменен на яркий премиальный золотой (`#FFD700`).
- **Видимость на Карте**: Непрозрачность (opacity) полигонов и толщина их границ увеличены. Убраны deprecation warnings.

## 3. Внедрение "WOW-момента" после пробежки [ВЫПОЛНЕНО]
- Экран эпичного завершения пробежки (`RunResultScreen`) реализован и показывается после сохранения маршрута.

## 4. Управление Ордами (Курс на социализацию)
- **Бэкенд готов**: База данных поддерживает создание Орд (`ordas`), привязку пользователей (`orda_id`), расчет очков влияния Орды и таблицу лидеров.
- **Что нужно доделать в приложении**:
  - Создать полноценный экран "Моя Орда".
  - Добавить интерфейс для **создания** новой Орды (выбор названия, цвета, логотипа).
  - Внедрить бонусы от Орды (общий рейтинг захвата города, клановые войны, бонусные XP за бег с соклановцами).
  - Добавить механику приглашения игроков (инвайт-ссылки) или поиска Орд для вступления.
  - Добавить чат внутри Орды (опционально).

## 5. Интеграция со Strava
- **Бэкенд готов**: Написаны вебхуки для автоматического получения пробежек со Strava, скрипты подписки.
- **Что нужно доделать в приложении**:
  - Настроить OAuth Flow в мобильном приложении (кнопка "Подключить Strava" в профиле).
  - Настроить автоматическое вытягивание старых пробежек при первом подключении.

## 6. Игровые Механики и Фид (Game Events) [ВЫПОЛНЕНО]
- **Бэкенд готов**: Таблица `game_events` фиксирует события (захват зоны, вступление в орду, кража территории).
- **Что нужно доделать в приложении**:
  - [x] Экран "Лента событий" (Activity Feed) реализован (`FeedModal`), пользователи видят захваты в реальном времени.

## 7. Переводы и Локализация [ВЫПОЛНЕНО]
- Основной интерфейс переведен на русский и английский.
